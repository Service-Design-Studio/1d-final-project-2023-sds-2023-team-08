<%= form_with(model: paynow) do |form| %>
  <% if paynow.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(paynow.errors.count, "error") %> prohibited this paynow from being saved:</h2>

      <ul>
        <% paynow.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
